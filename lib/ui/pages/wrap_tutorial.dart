import 'package:flutter/material.dart';

class WrapTutorial extends StatelessWidget {
  const WrapTutorial({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Wrap Tutorial"),
      ),
      body: const Center(
        child: Text("Wrap Tutorial"),
      ),
    );
  }
}
